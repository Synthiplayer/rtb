name: rtb
description: "Band Website"
publish_to: none
version: 1.0.0+1

environment:
  sdk: '>=3.8.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # UI & Routing
  go_router: ^15.2.0
  flutter_riverpod: ^2.6.1

  # Icons & SVG
  cupertino_icons: ^1.0.8
  flutter_svg: ^2.2.0
  web: ^1.1.1

  # Google Fonts (Rob​oto)
  google_fonts: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/logo.svg
    - assets/images/hero.jpeg

  fonts:
    - family: Airstream
      fonts:
        - asset: assets/fonts/AIRSTREA.ttf
